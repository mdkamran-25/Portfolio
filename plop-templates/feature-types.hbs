/**
 * {{pascalCase name}} Feature Types
 */

export interface {{pascalCase name}}Props {
  // Add your component props here
  id?: string;
  className?: string;
}

export interface {{pascalCase name}}Data {
  // Add your data model here
  id: string;
  name: string;
  createdAt: Date;
  updatedAt: Date;
}

{{#if withState}}
export interface {{pascalCase name}}State {
  // Add your state shape here
  data: {{pascalCase name}}Data[];
  loading: boolean;
  error: string | null;
  selectedId: string | null;
}

export interface {{pascalCase name}}Actions {
  // Add your actions here
  fetch{{pascalCase name}}s: () => Promise<void>;
  select{{pascalCase name}}: (id: string) => void;
  clear{{pascalCase name}}s: () => void;
}
{{/if}}

{{#if withApi}}
export interface {{pascalCase name}}ApiResponse {
  data: {{pascalCase name}}Data[];
  total: number;
  page: number;
  limit: number;
}

export interface {{pascalCase name}}CreateRequest {
  name: string;
  // Add other creation fields
}

export interface {{pascalCase name}}UpdateRequest {
  id: string;
  name?: string;
  // Add other update fields
}
{{/if}}
